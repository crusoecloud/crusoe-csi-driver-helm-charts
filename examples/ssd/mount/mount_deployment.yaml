---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: example-mount-ssd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: example-mount-ssd
  template:
    metadata:
      labels:
        app: example-mount-ssd
    spec:
      terminationGracePeriodSeconds: 0
      containers:
        - name: example-mount-ssd
          image: ubuntu:24.04
          command: ["sleep", "infinity"]
          volumeMounts:
            - mountPath: "/vol/myvolume"
              name: myvolume
      volumes:
        - name: myvolume
          persistentVolumeClaim:
            claimName: ssd-mount
            readOnly: false
